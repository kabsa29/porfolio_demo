<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Vaishnavi Agrawal – Portfolio</title>
  <meta name="description" content="Portfolio of Vaishnavi Agrawal – Developer • Researcher • ML Enthusiast" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #ffffff;
      --text: #0b0b0c;
      --muted: #666a73;
      --accent: #111827; /* navbar text */
      --ring: rgba(0,0,0,.08);
      --card: #f8f9fb;
      --border: #e6e8ee;
      --shadow: 0 10px 30px rgba(0,0,0,.08);
      /* Section brand colors from your spec */
      --english-green: #1B4D3E; /* About */
      --maroon: #800000;        /* Projects */
      --light-brown: #C4A484;   /* Contact background */
      --cream: #FFFDD0;         /* Contact text */
    }

    *, *::before, *::after { box-sizing: border-box; }
    html, body { height: 100%; scroll-behavior: smooth; }
    body {
      margin: 0;
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
      color: var(--text);
      background: var(--bg);
      line-height: 1.6;
    }

    /* Navbar */
    .nav {
      position: sticky; top: 0; z-index: 50;
      background: #fff;
      border-bottom: 1px solid var(--border);
      box-shadow: 0 6px 20px rgba(0,0,0,.04);
    }
    .nav-inner {
      max-width: 1100px; margin: 0 auto; display: flex; align-items: center; justify-content: space-between;
      padding: .75rem 1rem;
    }
    .brand { display: inline-flex; align-items: center; gap: .6rem; text-decoration: none; color: var(--accent); font-weight: 700; }
    .brand-badge { font-weight: 800; border: 2px solid #000; border-radius: 10px; padding: .15rem .4rem; display:inline-flex; align-items:center; justify-content:center; }
    .brand-badge code { font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; }

    .nav-links { display: flex; gap: .75rem; align-items: center; }
    .nav a.link { color: #000; text-decoration: none; font-weight: 600; padding: .5rem .75rem; border-radius: 12px; }
    .nav a.link.active, .nav a.link:hover { background: var(--card); }
    .menu-btn { display:none; background:#000; color:#fff; border:0; border-radius:12px; padding:.5rem .65rem; font-weight:700; }

    /* Sections */
    section { padding: 80px 16px; }
    .container { max-width: 1100px; margin: 0 auto; }

    /* Home */
    #home { padding-top: 96px; }
    .hero { display:grid; grid-template-columns: 1.2fr .8fr; gap: 32px; align-items: center; }
    .badge { display:inline-flex; align-items:center; gap:.5rem; font-size:.85rem; padding:.35rem .6rem; border-radius: 999px; background:#eef2ff; color:#1f2937; border:1px solid #e5e7eb; }
    .title { font-size: clamp(2rem, 4vw, 3rem); line-height:1.15; margin: .5rem 0 1rem; font-weight: 800; letter-spacing: -0.02em; }
    .subtitle { color: var(--muted); font-size: 1.05rem; }
    .cta { display:flex; gap:.75rem; margin-top: 1.25rem; }
    .btn { border:1px solid #000; background:#000; color:#fff; padding:.75rem 1rem; border-radius: 14px; text-decoration:none; font-weight:700; box-shadow: var(--shadow); }
    .btn.ghost { background:#fff; color:#000; }
    .hero-card { background: var(--card); border:1px solid var(--border); padding: 20px; border-radius: 20px; box-shadow: var(--shadow); }

    .quick-cards { margin-top: 28px; display:grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
    .quick-card { padding: 16px; border:1px solid var(--border); border-radius: 16px; background:#fff; }
    .quick-card h4 { margin:0 0 .25rem; }
    .tag { display:inline-block; font-size:.75rem; background:#f1f5f9; padding:.15rem .5rem; border-radius:999px; border:1px solid #e2e8f0; }

    /* About */
    #about { background: var(--english-green); color: #fff; }
    #about .lead { color: #d7f5e9; max-width: 70ch; }
    #about .grid { margin-top: 20px; display:grid; grid-template-columns: 1.2fr .8fr; gap: 24px; }
    .about-card { background: rgba(255,255,255,.06); border: 1px solid rgba(255,255,255,.2); padding: 18px; border-radius: 18px; }

    /* Projects */
    #projects { background: var(--maroon); color: #fff; }
    .project-toolbar { display:flex; gap:.5rem; flex-wrap:wrap; align-items:center; margin-bottom: 16px; }
    .chip { background: rgba(255,255,255,.1); border:1px solid rgba(255,255,255,.25); color:#fff; padding:.35rem .7rem; border-radius:999px; cursor:pointer; user-select:none; }
    .chip.active { background:#fff; color:#000; }
    .grid-projects { display:grid; gap:18px; grid-template-columns: repeat(3, 1fr); }
    .project-card { background: rgba(255,255,255,.08); border:1px solid rgba(255,255,255,.25); border-radius: 20px; overflow:hidden; transition: transform .2s ease, box-shadow .2s ease; }
    .project-card:hover { transform: translateY(-3px); box-shadow: 0 16px 40px rgba(0,0,0,.25); }
    .project-thumb { aspect-ratio: 16/9; background: linear-gradient(135deg, rgba(255,255,255,.25), rgba(255,255,255,.05)); display:flex; align-items:center; justify-content:center; font-weight:800; font-size:1.1rem; }
    .project-body { padding: 14px 16px 18px; }
    .project-title { margin:0 0 .25rem; font-size:1.1rem; font-weight:700; }
    .project-desc { margin:0 0 .6rem; opacity:.95; }
    .project-tags { display:flex; gap:.4rem; flex-wrap:wrap; }

    /* Contact */
    #contact { background: var(--light-brown); color: var(--cream); }
    #contact h2, #contact p, #contact label { color: var(--cream); text-shadow: 0 1px 0 rgba(0,0,0,.15); }
    .contact-grid { display:grid; grid-template-columns: 1fr 1fr; gap: 24px; }
    .contact-card { background: rgba(0,0,0,.08); border:1px solid rgba(255,255,255,.25); padding: 18px; border-radius: 18px; }
    .field { display:flex; flex-direction:column; gap:.4rem; margin-bottom: .9rem; }
    input, textarea { padding:.8rem .9rem; border-radius: 14px; border: 1px solid rgba(0,0,0,.25); font: inherit; background: #fff; color:#000; }
    textarea { min-height: 130px; resize: vertical; }
    .send-btn { background: var(--cream); color: #000; border: 1px solid #000; padding:.9rem 1.1rem; border-radius: 999px; font-weight: 800; cursor: pointer; }
    .send-btn:hover { filter: brightness(0.98); }
    .note { font-size:.9rem; opacity:.9; }

    /* Footer */
    footer { background: #0f172a; color: #e2e8f0; padding: 18px 16px; text-align:center; border-top:1px solid #0b1224; }
    footer a { color: #93c5fd; text-decoration: none; }

    /* Utilities */
    .section-title { font-size: clamp(1.6rem, 2.5vw, 2.2rem); margin: 0 0 .25rem; }
    .section-sub { color: #94a3b8; margin-top: 0; }

    /* Responsive */
    @media (max-width: 960px) {
      .hero { grid-template-columns: 1fr; }
      .quick-cards { grid-template-columns: repeat(2, 1fr); }
      .grid-projects { grid-template-columns: repeat(2, 1fr); }
      #about .grid, .contact-grid { grid-template-columns: 1fr; }
    }
    @media (max-width: 640px) {
      .nav-links { display:none; }
      .menu-btn { display:inline-block; }
      .quick-cards { grid-template-columns: 1fr; }
      .grid-projects { grid-template-columns: 1fr; }
      section { padding: 70px 12px; }
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="nav">
    <div class="nav-inner">
      <a class="brand" href="#home" aria-label="Go to home">
        <span class="brand-badge" aria-hidden="true"><code>&lt;/&gt;</code></span>
        <span>Vaishnavi Agrawal</span>
      </a>
      <div class="nav-links" id="navLinks">
        <a class="link" href="#home">Home</a>
        <a class="link" href="#about">About</a>
        <a class="link" href="#projects">Projects</a>
        <a class="link" href="#contact">Contact</a>
      </div>
      <button class="menu-btn" id="menuBtn" aria-label="Open menu">Menu</button>
    </div>
  </nav>

  <!-- Home -->
  <section id="home">
    <div class="container">
      <div class="hero">
        <div>
          <span class="badge">Developer • Researcher • ML Enthusiast</span>
          <h1 class="title">Hello! I'm Vaishnavi — I build clean, performant web apps and explore applied ML.</h1>
          <p class="subtitle">Currently a Data Scientist and final‑year CSE student. I love designing systems, writing thoughtful code, and turning ideas into real products.</p>
          <div class="cta">
            <a class="btn" href="#projects">See Projects</a>
            <a class="btn ghost" href="#contact">Hire Me</a>
          </div>

          <div class="quick-cards">
            <div class="quick-card">
              <h4>Highlights</h4>
              <p class="small">AWS, MERN, Java, Node.js, MongoDB, Flutter, REST APIs, Realtime chat.</p>
              <span class="tag">8.0 CGPA</span>
              <span class="tag">SDE Intern</span>
              <span class="tag">Oasis Infobyte</span>
            </div>
            <div class="quick-card">
              <h4>Research Lab</h4>
              <p class="small">Exploring recommender systems, scalable data pipelines, and graph‑based social features.</p>
              <span class="tag">Recsys</span>
              <span class="tag">Graphs</span>
              <span class="tag">LLMs</span>
            </div>
            <div class="quick-card">
              <h4>Open to Work</h4>
              <p class="small">Actively looking for SDE1/Backend roles. Available immediately.</p>
              <span class="tag">Full‑time</span>
              <span class="tag">Remote/BLR</span>
            </div>
          </div>
        </div>
        <div class="hero-card">
          <h3 style="margin-top:0">Snapshot</h3>
          <ul>
            <li>Major Project: MERN e‑commerce platform</li>
            <li>Project: JUNTO – full‑stack social platform</li>
            <li>DevOps: AWS CodePipeline • S3 hosting</li>
            <li>Focus: Java • DSA • System Design</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- About -->
  <section id="about">
    <div class="container">
      <h2 class="section-title">About Me</h2>
      <p class="section-sub">English green section with white font — as requested</p>
      <div class="grid">
        <div>
          <div class="about-card">
            <p class="lead">I'm a developer who enjoys building delightful user experiences backed by robust backends. I focus on Java, Node.js, and modern web stacks. I value clarity, performance, and maintainability.</p>
          </div>
          <div class="about-card">
            <h4 style="margin-top:0">What I do</h4>
            <ul>
              <li>Design and build RESTful APIs and data models</li>
              <li>Architect and ship full‑stack features (MERN)</li>
              <li>Automate deployments on AWS (S3/CodePipeline)</li>
              <li>Experiment with ML features to improve UX</li>
            </ul>
          </div>
        </div>
        <div>
          <div class="about-card">
            <h4 style="margin-top:0">Tech I enjoy</h4>
            <p>Java, Spring basics, Node.js, Express, MongoDB, React, Flutter, JWT/OAuth, WebSockets (planned), Redis (planned), graph databases (planned).</p>
          </div>
          <div class="about-card">
            <h4 style="margin-top:0">Certs & Achievements</h4>
            <ul>
              <li>Java Developer Intern – Oasis Infobyte</li>
              <li>Built JUNTO: auth, posts, real‑time chat, friend requests</li>
              <li>AWS Free‑Tier deployments with CI/CD</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Projects -->
  <section id="projects">
    <div class="container">
      <h2 class="section-title">Projects</h2>
      <p class="section-sub">Maroon section with white font — each project in its own box</p>

      <div class="project-toolbar" role="toolbar" aria-label="Project filters">
        <span class="chip active" data-filter="all">All</span>
        <span class="chip" data-filter="web">Web</span>
        <span class="chip" data-filter="ml">ML</span>
        <span class="chip" data-filter="mobile">Mobile</span>
      </div>

      <div class="grid-projects" id="projectGrid">
        <!-- Project: JUNTO -->
        <article class="project-card" data-tags="web">
          <div class="project-thumb">JUNTO</div>
          <div class="project-body">
            <h3 class="project-title">JUNTO – Social Platform</h3>
            <p class="project-desc">Full‑stack social app with auth, posts, friend requests, and real‑time chat (HTTP → WebSockets planned). Stack: Flutter • Node • MongoDB.</p>
            <div class="project-tags">
              <span class="tag">Flutter</span>
              <span class="tag">Node</span>
              <span class="tag">MongoDB</span>
            </div>
          </div>
        </article>

        <!-- Project: E‑commerce -->
        <article class="project-card" data-tags="web">
          <div class="project-thumb">MERN Shop</div>
          <div class="project-body">
            <h3 class="project-title">MERN E‑commerce</h3>
            <p class="project-desc">Major project featuring product catalog, cart, checkout, and admin dashboards. Deployed via AWS S3 + CodePipeline.</p>
            <div class="project-tags">
              <span class="tag">React</span>
              <span class="tag">Express</span>
              <span class="tag">AWS</span>
            </div>
          </div>
        </article>

        <!-- Project: Flip‑Pair -->
        <article class="project-card" data-tags="mobile">
          <div class="project-thumb">Flip‑Pair</div>
          <div class="project-body">
            <h3 class="project-title">Flip‑Pair – Memory Game</h3>
            <p class="project-desc">Adaptive‑timer memory card game with automated updates/hosting through AWS, leveraging Free Tier for efficient deployment.</p>
            <div class="project-tags">
              <span class="tag">Game</span>
              <span class="tag">AWS</span>
            </div>
          </div>
        </article>

        <!-- Project: Recsys Lab -->
        <article class="project-card" data-tags="ml">
          <div class="project-thumb">Recsys</div>
          <div class="project-body">
            <h3 class="project-title">Mini Recommender Lab</h3>
            <p class="project-desc">Toy pipelines for content‑based and collaborative filtering; experiments with embeddings to improve relevance.</p>
            <div class="project-tags">
              <span class="tag">Python</span>
              <span class="tag">Embeddings</span>
            </div>
          </div>
        </article>

        <!-- Placeholder project boxes -->
        <article class="project-card" data-tags="web">
          <div class="project-thumb">Portfolio</div>
          <div class="project-body">
            <h3 class="project-title">This Portfolio</h3>
            <p class="project-desc">Single‑file HTML, CSS & JS with responsive layout, color‑themed sections, and smooth navigation.</p>
            <div class="project-tags">
              <span class="tag">HTML</span>
              <span class="tag">CSS</span>
              <span class="tag">JS</span>
            </div>
          </div>
        </article>

        <article class="project-card" data-tags="ml">
          <div class="project-thumb">Vision</div>
          <div class="project-body">
            <h3 class="project-title">Vision Notes</h3>
            <p class="project-desc">Notes + demos on feature extraction and simple CNNs for study references.</p>
            <div class="project-tags">
              <span class="tag">CV</span>
              <span class="tag">CNN</span>
            </div>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact">
    <div class="container">
      <h2 class="section-title">Contact</h2>
      <p class="section-sub">Light brown background with cream‑colored font. Send button is cream with curved edges and black text.</p>
      <div class="contact-grid">
        <div class="contact-card">
          <form id="contactForm" novalidate>
            <div class="field">
              <label for="name">Your Name</label>
              <input id="name" name="name" type="text" placeholder="Enter your name" required />
            </div>
            <div class="field">
              <label for="email">Email</label>
              <input id="email" name="email" type="email" placeholder="you@example.com" required />
            </div>
            <div class="field">
              <label for="message">Message</label>
              <textarea id="message" name="message" placeholder="Tell me about your project…" required></textarea>
            </div>
            <button class="send-btn" type="submit">Send</button>
            <p class="note" id="formNote" role="status" aria-live="polite" style="margin-top:.6rem"></p>
          </form>
        </div>
        <div class="contact-card">
          <h4 style="margin-top:0">Reach me</h4>
          <p>Email: <a href="mailto:agrawalvaishnavi12@gmail.com">agrawalvaishnavi12@gmail.com</a></p>
          <p>Location: Khandwa, MP • India</p>
          <p>Open to: SDE1 / Backend / Full‑stack roles</p>
          <p class="note">I'll reply within 24–48 hours.</p>
        </div>
      </div>
    </div>
  </section>

  <footer>
    Made with ❤️ by Vaishnavi Agrawal
  </footer>

  <script>
    // Mobile menu toggle
    const menuBtn = document.getElementById('menuBtn');
    const navLinks = document.getElementById('navLinks');
    menuBtn?.addEventListener('click', () => {
      const shown = navLinks.style.display === 'flex';
      navLinks.style.display = shown ? 'none' : 'flex';
      navLinks.style.flexDirection = 'column';
      navLinks.style.gap = '0';
      [...navLinks.querySelectorAll('a')].forEach(a => { a.style.padding = '0.75rem 1rem'; a.style.borderRadius = '0'; a.style.borderTop = '1px solid var(--border)'; });
    });

    // Smooth scroll & active link highlighting
    const links = document.querySelectorAll('.nav a.link');
    links.forEach(link => {
      link.addEventListener('click', e => {
        // Close mobile menu on click
        if (window.innerWidth < 640) navLinks.style.display = 'none';
      });
    });

    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        const id = entry.target.getAttribute('id');
        const navLink = document.querySelector(`.nav a[href="#${id}"]`);
        if (entry.isIntersecting) {
          document.querySelectorAll('.nav a.link').forEach(a => a.classList.remove('active'));
          navLink?.classList.add('active');
        }
      });
    }, { rootMargin: '-40% 0px -55% 0px', threshold: 0 });

    document.querySelectorAll('section').forEach(section => observer.observe(section));

    // Project filter chips
    const chips = document.querySelectorAll('.chip');
    const cards = document.querySelectorAll('.project-card');
    chips.forEach(chip => chip.addEventListener('click', () => {
      chips.forEach(c => c.classList.remove('active'));
      chip.classList.add('active');
      const q = chip.dataset.filter;
      cards.forEach(card => {
        const tag = card.getAttribute('data-tags');
        const show = q === 'all' || (tag && tag.includes(q));
        card.style.display = show ? 'block' : 'none';
      });
    }));

    // Contact form (client‑side) validation + fake submit
    const form = document.getElementById('contactForm');
    const note = document.getElementById('formNote');
    form?.addEventListener('submit', (e) => {
      e.preventDefault();
      const data = new FormData(form);
      const name = data.get('name')?.toString().trim();
      const email = data.get('email')?.toString().trim();
      const message = data.get('message')?.toString().trim();

      if (!name || !email || !message) {
        note.textContent = 'Please fill out all fields.';
        return;
      }
      const emailOk = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
      if (!emailOk) { note.textContent = 'Please enter a valid email address.'; return; }

      // Simulate success
      note.textContent = 'Thanks! Your message has been sent.';
      form.reset();
    });
  </script>
</body>
</html>
